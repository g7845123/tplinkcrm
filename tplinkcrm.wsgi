import sys
 
sys.path.insert(0, '/var/www/tplinkcrm')
 
from tplinkcrm import app as application

# from werkzeug.debug import DebuggedApplication
# application = DebuggedApplication(application, True)

